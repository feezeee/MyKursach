@model AvailablePayment
@{
    Layout = "_Layout";
}

<form class="popup" asp-controller="AvailablePayment" asp-action="Create">
    <div class="popup-inner">
        <p class="addingWorkerPopup">Добавление доступного платежа</p>
        <ul class="popupinput">
            <li style="width:100%;">
                <span>Наименование доступного платежа*</span>
                <input asp-for="AvailablePaymentName" required type="text" placeholder="Введите наименование доступого платежа" />
                @Html.ValidationMessageFor(model => model.AvailablePaymentName, null, new { style = "color:red" })
            </li>
            
        </ul>
        <a type="button" asp-action="List" asp-controller="AvailablePayment" class="closepopup">X</a>
        <div class="popup_button">
            <button class="popupsubmit" type="submit">
                Создать
            </button>
            <button class="popupclear" type="reset">
                Очистить
            </button>
        </div>
    </div>
</form>

