@model MyKursach2.Models.Gender
@{
    Layout = "_Layout";
}

<script src="@Url.Content("~/lib/jquery-validation/dist/jquery.validate.js")" type="text/javascript"></script>
<script src="@Url.Content("~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.js")" type="text/javascript"></script>

<form class="popup" asp-controller="Gender" asp-action="Edit">
    <div class="popup-inner">
        <p class="addingWorkerPopup">Изменение пола</p>
        <ul class="popupinput">
            <li style="width:100%;">
                <span>Наименование пола*</span>
                <input type="hidden" name="Id" value="@Model.Id" />
                <input asp-for="GenderName" required type="text" placeholder="Введите наименование пола" value="@Model.GenderName" />
                @Html.ValidationMessageFor(model => model.GenderName, null, new { style = "color:red" })
            </li>           
        </ul>
        <a type="button" asp-action="List" asp-controller="Gender" class="closepopup">X</a>
        <div class="popup_button">
            <button class="popupsubmit" type="submit">
                Сохранить
            </button>                
            <a class="popupdelete" type="button" asp-action="Delete" asp-controller="Gender" asp-route-id=@Model.Id>Удалить</a>
            
        </div>

    </div>
</form>

